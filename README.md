# Dream

A lazily evaluated functional language with a self-hosted compiler and a C++ VM.

Dream targets Linux, Windows, and macOS. Compiled `.dream` images are intended
to run unchanged across all three, except for explicitly platform-specific
operations. See [platform support and verification](docs/platforms.md) and the
[language reference](docs/language-spec.md).
